name: Test detect-branch-version
description: Tests for detect-branch-version.

runs:
  using: composite
  steps:
    - id: detect-branch-version
      uses: ./actions/detect-branch-version

    - uses: ./actions/run-velvet-code
      with:
        code: |
          >> 'Action outputs' {
            >> 'branch' {
              put '${{ steps.detect-branch-version.outputs.branch }}' |
                should-not-be-empty
            }

            >> 'version' {
              put '${{ steps.detect-branch-version.outputs.version }}' |
                should-not-be-empty
            }

            >> 'escaped version' {
              put '${{ steps.detect-branch-version.outputs.escaped-version }}' |
                should-contain \.
            }

            >> 'major component' {
              put '${{ steps.detect-branch-version.outputs.major }}' |
                should-not-be-empty
            }
          }
