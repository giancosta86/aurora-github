name: Test run-velvet-code
description: Tests for run-velvet-code.

runs:
  using: composite
  steps:
    - name: Setup Elvish context
      uses: ./actions/setup-elvish-context

    - uses: ./actions/run-velvet-code
      with:
        code: |
          >> "Basic test" {
            echo Hello, world!
            fail-test
          }
